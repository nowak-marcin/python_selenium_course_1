# 1) pytest-html:
# terminal: pip install pytest-html
# terminal: pytest --html=my_report.html --self-contained-html


# 2) allure framework:
# terminal: pip install allure-pytest
# terminal: pytest --alluredir=allure_reports
# terminal: allure serve <path to allure_reports
